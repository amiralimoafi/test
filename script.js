/*let n = prompt("عددی که میخواهید روی ان عمل فاکتوریل انجام شود را وارد کنید ")


function factorial(n) {
    let h = 1;
    for (let i = 2; i <= n; i++) {
        h *= i;
    }
    return h;
}
alert(factorial(n)); 
*/

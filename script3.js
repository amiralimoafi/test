/*
let word1 = prompt("کلمه اول:");
let word2 = prompt("کلمه دوم:");
 
let words = [];
words.push(word1, word2);

for (let i = 0; i < words.length; i++) {
  words[i] = words[i][0].toUpperCase() + words[i].substr(1);
}

alert(words.join(" افرین "));
*/


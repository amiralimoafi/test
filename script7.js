/*
let arayeh = [];
let pramt;

do {
    pramt = prompt('کلمه مورد نظر را وارد کنید\nبرای پایان انتخاب‌ها عدد ۱ را وارد کنید');
    if (pramt !== "1") { 
        arayeh.push(pramt);
    }
} while (pramt !== "1");

let finaly = arayeh.map(word => word.toUpperCase());

alert(finaly);
*/